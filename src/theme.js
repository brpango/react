import { createTheme } from "@mui/system";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1760a5",
      light: "skyblue",
    },
    secondary: "15c630",
  },
  otherColor: "#999",
});

export default theme;
